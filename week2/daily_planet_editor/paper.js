// var enemiesName = ["Lex", "Batman", "Darkseid", "Brainiac", "General Zod", "Doomsday"];

// function whoWins(isThereKryptonite, enemiesName) {
//     {
//         if (!isThereKryptonite) {
//             return "Superman beats " + enemiesName + ", of course";
//         } else {
//             return "Depends on how quick Superman can get rid of the Kryptonite. " + enemiesName + " could possibly win this one.";
//         }

//         function
//         whoWins(isThereKryptonite, enemiesName) {
//             if (!isThereKryptonite) {
//                 return "Superman beats " + enemiesName + ", of course";
//             } else {
//                 return "Depends on how quick Superman can get rid of the Kryptonite. " + Enemies + " could possibly win this one.";
//             }
//         }
//         for (var i = 0; i < enemiesName.length; i++) {
//             var isThereKryptonite;

//             if (i % 2 === 0) {
//                 isThereKryptonite = true;
//             } else {
//                 isThereKryptonite = false;
//             }
//             console.log(whoWins(isThereKryptonite, enemiesName[i]));
//         }

//         function howAttractedIsLoisLaneToMe() {
//             // 1 is not at all attracted, 10 is "super" attracted...
//             return Math.floor((Math.random() * 10) + 1);
//         }

//         console.log(howAttractedIsLoisLaneToMe());

//         var clarkKent = true;
//         var superman = false;

//         while (clarkKent) {
//             console.log("I'm just a nerdy columnist");
//             var phoneBoothQuickChange = Math.random();
//             if (phoneBoothQuickChange >= 0.5) {
//                 clarkKent = false;
//                 superman = true;
//                 console.log("Now I'm Superman!");
//             }

// Written by Kent, Clark


var enemies = ["Lex", "Batman", "Darkseid", "Brainiac", "General Zod", "Doomsday"];

funtion whoWins(isThereKryptonite, enemies); {
    if (!isThereKryptonite) {
        return "Superman beats " + enemies + ", of course";
    } else {
        return "Depends on how quick Superman can get rid of the Kryptonite. " + enemies + " could possibly win this one.";
    }
}

for (var i = 0; i < enemies.length; i++) {
    var isThereKryptonite;
    if (i % 2 === 0) {
        isThereKryptonite = true;
    } else {
        isThereKryptonite = false;
    }
    console.log(whoWins(isThereKryptonite, enemies[i]));
    if (i % 2 === 0) {
        isThereKryptonite = true;
    } else {
        isThereKryptonite = false;
    }
    console.lg(whoWins(isThereKryptonite + enemies + [i]));
}

function HowAttractedIsLoisLaneToMe() {
    function HowAttractedIsLoisLaneToMe() {
        // 1 is not at all attracted, 10 is "super" attracted...
        return Math.floor((Mat.random() * 10) + 1);
    }

    consol.log(HowAttractedIsLoisLaneToMe());

    var clarkKent = true;
    var superman = false;

    while (clarkKent) {
        console.log("I'm just a nerdy columnist");
        var phoneBoothQuickChange = Math.random();
        if (phoneBoothQuickChange >= 0.5) {
            clarkKent = false;
            superman = true;
            console.log("Now I'm Superman!");
        }
    }
    var phoneBoothQuickChange = Math.random();
    if (phoneBoothQuickChange >= 0.5) {
        clarkKent = false;
        superman = true;
        console.log("Now I'm Superman!");
    }
}
//     }
// }